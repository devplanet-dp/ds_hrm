name: ds_hrm
description: The official app for Divisional Secretariat  - Bandarawela

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  easy_localization:
  #stacked service
  stacked:
  stacked_services:
  stacked_themes:

  #firebase
  firebase_core:
  firebase_auth:
  cloud_firestore:
  firebase_storage:

  image_picker:
  google_fonts:
  #date
  flutter_datetime_picker:
  carousel_slider:

  rxdart:





dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/translation/
