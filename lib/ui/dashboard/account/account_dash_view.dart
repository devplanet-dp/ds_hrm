import 'package:flutter/material.dart';

class AccountDashView extends StatelessWidget {
  const AccountDashView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
